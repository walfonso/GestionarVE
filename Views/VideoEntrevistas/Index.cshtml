@model IEnumerable<GestionarVE.VideoEntrevista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Estado.Descrption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SolicitudPersonal.Puesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Estado.Descrption)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SolicitudPersonal.Puesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VEntrevistaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VEntrevistaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VEntrevistaID })
        </td>
    </tr>
}

</table>
