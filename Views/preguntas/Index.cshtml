@model IEnumerable<GestionarVE.pregunta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VideoEntrevista.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoentrevistaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orderPregID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VideoEntrevista.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoentrevistaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orderPregID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.preguntaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.preguntaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.preguntaID })
        </td>
    </tr>
}

</table>
